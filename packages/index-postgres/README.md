# @mnem/index-postgres

PostgreSQL-backed index adapter for Mnem. Uses `pgvector` for semantic similarity search and `tsvector` full-text search.

Designed for multi-tenant deployments that already run a shared Postgres instance.

Implementation scheduled for Mnem v0.1.

## License

MIT
