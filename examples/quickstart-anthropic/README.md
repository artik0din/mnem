# Example: Quickstart with Anthropic

Minimal example showing how to prepare a Mnem vault and wire it to an Anthropic tool adapter via `@mnem/tools`.

```bash
pnpm install
VAULT_ROOT=./vault pnpm --filter example-quickstart-anthropic start
```
