# Example: Quickstart with OpenAI

Minimal example showing how to set up a Mnem vault on local disk and branch it to an OpenAI embedding provider.

```bash
pnpm install
VAULT_ROOT=./vault pnpm --filter example-quickstart-openai start
```
