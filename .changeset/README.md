# Changesets

This directory is used by [Changesets](https://github.com/changesets/changesets) to coordinate version bumps and changelog entries across the Mnem monorepo.

To add a changeset:

```bash
pnpm changeset
```

Then follow the prompts to select the packages affected and the semver bump type.
